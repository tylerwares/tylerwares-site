// This file is no longer used as the AI features have been removed for easier deployment.
// Keeping the file placeholder to prevent import errors if referenced elsewhere during transition.

export const generateChatResponse = async () => {
  return "AI features are currently disabled.";
};